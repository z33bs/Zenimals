<?xml version="1.0" encoding="UTF-8"?>
<ContentView
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:Zenimals.Controls"
    xmlns:views="clr-namespace:Zenimals.Views"    
    x:Name="self"
    x:Class="Zenimals.Views.AnimalsView"
    
    xmlns:mvvm="clr-namespace:ZenMvvm;assembly=ZenMvvm"
    mvvm:ViewModelLocator.WireSpecificViewModel="AnimalsViewModel"
>
    <CollectionView Margin="20"
                    ItemsSource="{Binding Data, Source={x:Reference self}}"
                    ItemTemplate="{StaticResource AnimalTemplate}"
                    SelectionMode="Single"
                    x:Name="AnimalsCollectionView"
                    SelectionChangedCommand="{Binding BindingContext.SelectionChangedCommand, Source={x:Reference Name=AnimalsCollectionView}}"
                    SelectionChangedCommandParameter="{Binding ., Source={x:Reference Name=AnimalsCollectionView}}"
                    StyleId="dogdetails"/>
</ContentView>
